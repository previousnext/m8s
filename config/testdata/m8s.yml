namespace: test

retention: 30m

auth:
  user: nick
  pass: rocks

build:
  container: app
  steps:
    - echo 1
    - echo 2

cache:
  type: standard
  paths:
    - /root/.composer

secrets:
  dockercfg: test-dockercfg
  ssh: test-ssh